{
    "scripts": {
        "install": "cd src/UI && npm install",
        "build": "cd src/UI && npm run build",
        "dev": "cd src/UI && npm run dev",
        "ci": "cd src/UI && npm ci",
        "prettier": "cd src/UI && npm run prettier"
    }
}

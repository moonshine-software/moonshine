export interface TheButtonProps {
    icon?: string,
    xs?: boolean,
    sm?: boolean,
    md?: boolean,
    lg?: boolean,
    inset?: boolean,
    square?: boolean,
    brand?: boolean,
    secondary?: boolean,
    warning?: boolean,
    success?: boolean,
    danger?: boolean,
    outline?: boolean,
    text?: boolean,
    reactive?: boolean
}
